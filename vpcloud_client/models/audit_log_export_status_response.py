# coding: utf-8

"""
    Voltage Park Cloud API

      ⚠️ **BETA PREVIEW**: This API is currently in beta and is subject to changes. Features, endpoints, request/response formats, and functionality may be modified as we continue to improve the service.      Bare Metal and VM provisioning and management services.

    The version of the OpenAPI document: 1.0.0-beta
    Contact: support@voltagepark.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class AuditLogExportStatusResponse(BaseModel):
    """
    State of the caller's 90-day audit export: which window it covers, whether the archive is ready, and - once it is - a short-lived presigned download URL.
    """ # noqa: E501
    status: StrictStr = Field(description="Where the export stands. 'notStarted': no archive for today's window and nothing running - POST to start one. 'running': a build is in progress, poll again in a few seconds. 'ready': the archive is complete and 'url' is present. 'failed': the build did not finish, see 'error', and POST to retry.  **Which operation returns which:** POST only ever returns 'running' or 'ready' - starting an export always leaves it in one of those two states. All four are possible from GET. 'notStarted' is therefore GET-only, and you will see it when you check before any export has been requested, and again after each UTC midnight, when the previous day's archive no longer covers the current window.")
    window_start: datetime = Field(description="Inclusive start of the exported window: a UTC midnight, 90 days before 'windowEnd'.", alias="windowStart")
    window_end: datetime = Field(description="Exclusive end of the exported window: the most recent UTC midnight. The current partial day is not included.", alias="windowEnd")
    format: StrictStr = Field(description="Archive encoding: gzip-compressed newline-delimited JSON, one audit event per line.")
    url: Optional[StrictStr] = Field(default=None, description="Presigned HTTPS URL for the gzipped NDJSON archive. Present only when 'status' is 'ready'. Treat it as a credential: anyone holding it can download the archive until it expires.")
    expires_at: Optional[datetime] = Field(default=None, description="When 'url' stops working (RFC 3339 / ISO 8601, UTC). Present only when 'status' is 'ready'. Minutes, not hours - call the endpoint again for a fresh URL. The archive itself outlives the URL.", alias="expiresAt")
    started_at: Optional[datetime] = Field(default=None, description="When the current (or most recent) build started. Absent when 'status' is 'notStarted'.", alias="startedAt")
    completed_at: Optional[datetime] = Field(default=None, description="When the build finished, successfully or not. Present only when 'status' is 'ready' or 'failed'.", alias="completedAt")
    error: Optional[StrictStr] = Field(default=None, description="Short, non-sensitive reason the build failed. Present only when 'status' is 'failed'.")
    __properties: ClassVar[List[str]] = ["status", "windowStart", "windowEnd", "format", "url", "expiresAt", "startedAt", "completedAt", "error"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['notStarted', 'running', 'ready', 'failed']):
            raise ValueError("must be one of enum values ('notStarted', 'running', 'ready', 'failed')")
        return value

    @field_validator('format')
    def format_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['ndjson+gzip']):
            raise ValueError("must be one of enum values ('ndjson+gzip')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AuditLogExportStatusResponse from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AuditLogExportStatusResponse from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "status": obj.get("status"),
            "windowStart": obj.get("windowStart"),
            "windowEnd": obj.get("windowEnd"),
            "format": obj.get("format"),
            "url": obj.get("url"),
            "expiresAt": obj.get("expiresAt"),
            "startedAt": obj.get("startedAt"),
            "completedAt": obj.get("completedAt"),
            "error": obj.get("error")
        })
        return _obj


