# coding: utf-8

"""
    Voltage Park Cloud API

      ⚠️ **BETA PREVIEW**: This API is currently in beta and is subject to changes. Features, endpoints, request/response formats, and functionality may be modified as we continue to improve the service.      Bare Metal and VM provisioning and management services.

    The version of the OpenAPI document: 1.0.0-beta
    Contact: support@voltagepark.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class StorageView(BaseModel):
    """
    Harbor's typed view of a single physical VAST storage view (a mount point and its quotas). Sizes are in bytes; Thundercat's unit/wire format is not exposed to callers.
    """ # noqa: E501
    mount_point: StrictStr = Field(description="Mount point for the storage view (e.g., '/data', '/home').", alias="mountPoint")
    size: Optional[StrictInt] = Field(default=None, description="Provisioned size in bytes.")
    soft_limit: Optional[StrictInt] = Field(default=None, description="Soft quota in bytes, when set.", alias="softLimit")
    hard_limit: Optional[StrictInt] = Field(default=None, description="Hard quota in bytes, when set.", alias="hardLimit")
    state: Optional[StrictStr] = Field(default=None, description="Lifecycle state of the storage view on the backing VAST tenant. Callers should treat `active` as the only \"ready to use\" state; `provisioning` and `modifying` are transient and callers that just issued a 202 add/update should poll `GET .../storage/views/{view}` until `state == \"active\"` (or `unhealthy` to fail fast). The `terminating` state is observable when a delete is in progress or during fleet teardown. The field is optional and will be **absent** from the response for three reasons: (1) the upstream Thundercat response omitted it (older deployments); (2) Thundercat returned a lifecycle value Harbor does not recognize (forward-compatibility with future upstream enum members — the raw value is captured in Harbor logs); (3) the PATCH refresh-GET failed and Harbor fell back to a synthesized response body. In all three cases callers should treat an absent `state` as \"unknown, please re-GET\". Additional caveat: on a POST 202 response served via idempotency-key replay, `state` reflects the original request's timepoint (typically `provisioning`), not the current lifecycle state; re-`GET` to obtain authoritative state.")
    __properties: ClassVar[List[str]] = ["mountPoint", "size", "softLimit", "hardLimit", "state"]

    @field_validator('state')
    def state_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['provisioning', 'modifying', 'active', 'terminating', 'unhealthy']):
            raise ValueError("must be one of enum values ('provisioning', 'modifying', 'active', 'terminating', 'unhealthy')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of StorageView from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if size (nullable) is None
        # and model_fields_set contains the field
        if self.size is None and "size" in self.model_fields_set:
            _dict['size'] = None

        # set to None if soft_limit (nullable) is None
        # and model_fields_set contains the field
        if self.soft_limit is None and "soft_limit" in self.model_fields_set:
            _dict['softLimit'] = None

        # set to None if hard_limit (nullable) is None
        # and model_fields_set contains the field
        if self.hard_limit is None and "hard_limit" in self.model_fields_set:
            _dict['hardLimit'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of StorageView from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "mountPoint": obj.get("mountPoint"),
            "size": obj.get("size"),
            "softLimit": obj.get("softLimit"),
            "hardLimit": obj.get("hardLimit"),
            "state": obj.get("state")
        })
        return _obj


